function GalleryList({galleryList}) {
  return (
    <ul>
    {galleryList.map((gallery) => {
      return(
        <li key={gallery. /* name */}>
          {gallery./* name */}
          {gallery./* name */}
          {gallery./* name */}
          {gallery./* name */}
        </li>
      );
      })}
    </ul>
  );
}